"""  Comments in .vimrc start with a double-quote

" Turn on hilighted search results
"set hlsearch

" Fixes oddities with syntax highlighting on multi-line syntactic units
"syntax sync fromstart

" Turn on 'console' fenced code block syntax highlighting in vim/gvim Markdown.
let g:markdown_fenced_languages = ['console']

" You might have other language requirements:
"let g:markdown_fenced_languages = ['console', 'sh', 'c', 'php', 'test']

